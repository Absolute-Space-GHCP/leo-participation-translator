# ============================================================================
# Cursor Ignore — Hide noisy files from the IDE file explorer and AI context
# ============================================================================

# Session auto-logs (generated by claude-mem / session workflow)
sessions/session_*.log
sessions/.current_session.log
sessions/.session_started

# Test outputs (generated, not source code)
data/test-outputs/

# Node modules (already in .gitignore but also hide from AI)
node_modules/

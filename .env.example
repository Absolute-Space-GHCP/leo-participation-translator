# ============================================================================
# Environment Variables Template - The Participation Translator
# ============================================================================
# Version:     1.0.1
# Updated:     2026-02-03
# Purpose:     Template for local environment variables
# Usage:       Copy to .env and fill in values (never commit .env!)
# ============================================================================

# ===================
# Application
# ===================
NODE_ENV=development
LOG_LEVEL=debug
PORT=3000

# ===================
# Google Cloud Platform
# ===================
# GCP Project for Vertex AI, Firestore, Cloud Storage
GCP_PROJECT_ID=participation-translator
GCP_REGION=us-central1

# Service account (optional - prefer gcloud auth for local dev)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ===================
# Vertex AI - Claude
# ===================
# Claude is accessed via Vertex AI
# Region for Claude on Vertex (currently us-east5 or europe-west1)
VERTEX_AI_CLAUDE_REGION=us-east5
VERTEX_AI_CLAUDE_MODEL=claude-opus-4-6

# ===================
# Vertex AI - Embeddings
# ===================
VERTEX_AI_EMBEDDING_MODEL=text-embedding-005
VERTEX_AI_EMBEDDING_DIMENSIONS=768

# ===================
# Vector Store (Firestore)
# ===================
# Uses Cloud Firestore with vector embeddings — no separate config needed.
# Firestore uses ADC credentials automatically.

# ===================
# Cultural Intelligence APIs
# ===================
# See docs/CULTURAL_INTELLIGENCE.md for full research and alternatives

# Semantic Web Search (DUAL INTEGRATION)
# Both services are used and results are merged
# ⚠️  Keys are stored in GCP Secret Manager (encrypted).
#     The app reads them automatically via src/lib/secrets/index.ts.
#     Only set these for local dev if Secret Manager is unreachable:
# EXA_API_KEY=<from Secret Manager: exa-api-key>
# TAVILY_API_KEY=<from Secret Manager: tavily-api-key>

# Search + Summarization (FAILOVER ARCHITECTURE)
# Primary: Gemini with Grounding (uses ADC, no separate key)
# Failover: Perplexity
PERPLEXITY_API_KEY=your-perplexity-api-key

# ===================
# Future: Additional APIs (not yet implemented)
# ===================
# REDDIT_CLIENT_ID=your-reddit-client-id
# REDDIT_CLIENT_SECRET=your-reddit-client-secret
# PERPLEXITY_API_KEY=<from Secret Manager when added>

# ===================
# Firebase / Firestore
# ===================
# Firestore is used for session storage and cultural data cache
# Uses ADC by default - no additional config needed for local dev

# ===================
# Cloud Storage
# ===================
# Bucket for document storage and exports
GCS_DOCUMENTS_BUCKET=participation-translator-documents
GCS_EXPORTS_BUCKET=participation-translator-exports

# ===================
# Future: Integrations (not yet implemented)
# ===================
# TREND_WEBHOOK_SECRET=your-webhook-secret
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ===================
# Authentication
# ===================
# Cloud IAP client ID (for production)
# IAP_CLIENT_ID=your-iap-client-id.apps.googleusercontent.com

# ===================
# Development Only
# ===================
# Skip auth checks in development
SKIP_AUTH=true

# Enable debug logging for specific modules
# DEBUG=participation:*
